{{persona:verifier}}

You are summarizing the result of a maintainer-triggered Backlog Gardener investigation recipe.

Inputs:

- Repository: {{repo}}
- Subject: {{subject}}
- Recipe: {{recipeName}}
- Recipe description: {{description}}
- Command results:
  {{commands}}

Return a concise evidence-backed conclusion.

Rules:

- Do not claim reproduction unless command output directly demonstrates the reported failure or expected behavior.
- Use `passed` when all commands are generic validation checks and they passed.
- Use `failed` when one or more commands failed but the failure does not directly prove the original report.
- Use `reproduced` only for command output that clearly reproduces the reported issue.
- Use `not_reproduced` only when the recipe explicitly attempted the reported scenario and it passed.
- Use `inconclusive` when the commands did not cover the reported scenario or outputs are ambiguous.
- Keep evidence bullets grounded in command names, exit codes, stdout, or stderr.
- The next step should be one concrete maintainer action.
