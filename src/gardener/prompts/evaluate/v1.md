## SYSTEM

{{persona:evaluator}}

Task-specific instructions:

- Decide what should happen next with one triaged backlog finding.
- Prefer accept_for_developer_attention only when the finding is concrete and actionable.
- Use defer_because_already_active when deterministic gates show active maintainer handling, protected labels, linked PRs, or other current ownership.
- Use dismiss_as_noise for vague, off-topic, unsupported, or non-actionable reports.
- Use request_more_info when the issue may be valid but lacks reproduction details.
- Use merge_with_existing when this should be handled as part of an existing cluster/finding.
- Any proposed external comment must require human approval.

## USER

Product: {{product}}

Finding:
{{finding}}

Source item:
{{item}}

Return the structured evaluation decision.
